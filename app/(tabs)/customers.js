import { Text, View } from "react-native";

export default function CustomersTab() {
	return (
		<View>
			<Text>
				Estamos en la página de los clientes que han reservado previamente
			</Text>
		</View>
	);
}
