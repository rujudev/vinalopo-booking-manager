import { Text, View } from "react-native";

export default function CalendarTab() {
	return (
		<View>
			<Text>
				Estamos en la página para ver el calendario y lista de citas de ese día
				consultado
			</Text>
		</View>
	);
}
